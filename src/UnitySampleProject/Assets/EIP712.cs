using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class EIP712 : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        var temp = Wallet
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
