name: Duplicate Package Samples Workflow

on:
  pull_request:
    branches: [ main ]

jobs:
  duplicate:
    name: Duplicate samples job 👯
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Duplicate Web3 Unity Samples
      run: |
        for entry in "${DUPLICATE_SAMPLE_VARS[@]}"
        do
          IFS=':' read -ra dirs <<< "$entry"
          export SOURCE_PATH=${dirs[0]}
          export DESTINATION_DIRECTORY=${dirs[1]}
          
          SOURCE_PATH="${GITHUB_WORKSPACE}/$SOURCE_PATH/$(ls "$SOURCE_PATH" | head -n 1)/$DESTINATION_DIRECTORY"
        
          export DESTINATION_PATH=${dirs[2]}
          
          rm -rf "$DESTINATION_PATH"
          mkdir -p "$DESTINATION_PATH"
          cp -r "$SOURCE_PATH" "$DESTINATION_PATH"
        done
    - name: Commit and Push
      run: |
        git config user.email $git_email
        git config user.name "${{ github.actor }}"
        git add ${GITHUB_WORKSPACE}/Packages/io.chainsafe.web3-unity/Samples~/. -f
        git add ${GITHUB_WORKSPACE}/Packages/io.chainsafe.web3-unity.web3auth/Samples~/. -f
        git diff-index --cached --quiet HEAD || git commit -m "Auto-duplicate Packages Samples"
        git push
      env:
        git_email: "${{ github.actor }}@users.noreply.github.com"