name: Sync Branches

on:
  workflow_call:
    inputs:
      base:
        description: "Base branch to Sync"
        required: true
        type: string
        default: "main"
      target:
        description: "Target branch to Sync"
        required: true
        type: string
        default: "dev"
  workflow_dispatch:
    inputs:
      base:
        required: true
        description: "Base branch to Sync"
        type: string
      target:
        required: true
        description: "Target branch to Sync"
        type: string

jobs:
  setup:
    name: Sync Branches ♾️
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.inputs.target }}
          lfs: true
          ssh-key: ${{ secrets.DEPLOY_KEY }}
      - name: Sync Branches
        run: |
          git config user.email $git_email
          git config user.name $git_actor
          git branch --show-current
          echo "Syncing branches... base: ${{ github.event.inputs.base }} target: ${{ github.event.inputs.target }}"
          echo "Syncing branches... second check base: ${{ github.inputs.base }} target: ${{ github.inputs.target }}"
          echo "Syncing branches... third check base: ${{ inputs.base }} target: ${{ inputs.target }}"
          git merge ${{ github.event.inputs.base }} --allow-unrelated-histories
          git push
        shell: bash
        env:
          git_email: "${{ github.actor }}@users.noreply.github.com"
          git_actor: "${{ github.actor }}"