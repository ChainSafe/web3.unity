public async Task<{OUTPUT_PARAM}> {METHOD_NAME}({INPUT_PARAMS}) 
{
    var response = await {FUNCTION_CALL}({METHOD_NAME}, new object [] {
        {INPUT_PARAM_NAMES}
    });
    
    return {OUTPUT_PARAM_TYPECAST};
}